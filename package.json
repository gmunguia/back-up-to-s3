{
  "name": "backup-immich-files",
  "module": "index.ts",
  "scripts": {
    "bundle": "esbuild cli.ts --bundle --platform=node --packages=external > cli.js",
    "bundle:test": "esbuild back-up.test.ts --bundle --platform=node --packages=external > back-up.test.js",
    "test": "npm run bundle:test && mocha *.test.js",
    "test:debug": "npm run bundle:test && node --inspect-brk node_modules/.bin/mocha -t 0 *.test.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.414.0",
    "@aws-sdk/lib-storage": "^3.414.0",
    "envalid": "^8.0.0",
    "glob": "^10.3.5",
    "tar": "^6.2.0"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.1",
    "@types/s3rver": "^3.7.0",
    "@types/tar": "^6.1.6",
    "bun-types": "latest",
    "esbuild": "^0.19.3",
    "expect": "^29.7.0",
    "mocha": "^10.2.0",
    "prettier": "^3.0.3",
    "s3rver": "^3.7.1",
    "typescript": "^5.2.2"
  }
}
